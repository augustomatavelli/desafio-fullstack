import styled from 'styled-components'

export const StyledContainerModal = styled.form`
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: 1rem;
    border-radius: 5px;
    border: 2px solid var(--color-grey-0);
    padding: 1.5rem;
    width: 100%;

    div {
        display: flex;
        gap: 3rem;
    }
`