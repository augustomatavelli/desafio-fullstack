import { LoginForm } from "../../components/LoginForm"
import { Container } from "../../styles/container"

export const Login = () => {
    return (
        <Container>
            <main>
                <LoginForm />
            </main>
        </Container>
    )
}